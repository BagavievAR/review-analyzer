version: "3.9"

services:
  web:
    build: .
    container_name: review-analyzer-web
    ports:
      - "5000:5000"
    environment:
      DATABASE_PATH: /app/data/reviews.db
      FLASK_RUN_HOST: 0.0.0.0
      FLASK_RUN_PORT: 5000
    volumes:
      - ./data:/app/data